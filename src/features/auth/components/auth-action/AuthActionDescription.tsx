export const AuthActionDescription = () => {
  return (
    <p className="mt-5 max-w-2xl text-base leading-7 text-slate-300">
      TMDB owns the user accounts, so account sign in happens on TMDB. Your app
      only stores the returned session and uses it for features like watchlists,
      favourites and ratings.
    </p>
  )
}
